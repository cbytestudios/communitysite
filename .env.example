# Database (Required)
DATABASE_URL="file:./prisma/dev.db"

# JWT Secret for authentication (Required)
JWT_SECRET=your-secure-random-string-here

# All other settings are configured through the admin panel after setup!